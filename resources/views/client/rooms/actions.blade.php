<a class="btn btn-primary makeReservationBtn" title="make a reservation" value="{{ $id }}"
   href="{{ route('make.reservation', $id) }}" data-datatable="#AvailableDatatable">
    <i class="fas fa-plus"></i>
    Make Reservation
</a>
